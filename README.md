# mlflow-production-stack
Despliegue de producción del servidor de tracking MLflow usando Docker Compose. El stack está protegido por Nginx con TLS (Let's Encrypt) y autenticación HTTP Basic, respaldado por PostgreSQL para persistencia de metadatos.
